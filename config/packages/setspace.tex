% Giãn dòng nhẹ để nội dung bài học thoáng hơn mặc định.
\setstretch{1.125}

% Giữ tối thiểu 2pt giữa các dòng có nội dung cao.
\setlength{\normallineskiplimit}{2pt}
\setlength{\normallineskip}{2pt}
\setlength{\lineskiplimit}{\normallineskiplimit}
\setlength{\lineskip}{\normallineskip}
